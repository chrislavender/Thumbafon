source 'https://github.com/CocoaPods/Specs.git'

platform :ios, "9.0"

# use_frameworks!

xcodeproj 'Thumbafon'

# pod 'AFNetworking'
# pod 'OHHTTPStubs' # Stub RESTful calls
# pod 'SDWebImage' # UIImageView caching
# pod 'SVProgressHUD' # a clean UIActivityView HUD
# pod 'SnapKit'
# pod 'XCGLogger'

# Unit Testing
# we don't want to include Kiwi with our regular builds so we need a separate config for KareoTests
# target :ThumbafonTests, :exclusive => true do
#   pod 'Kiwi'

# add here to avoid warnings on the Test build scheme
#   pod 'SVProgressHUD' # a clean UIActivityView HUD
#   pod 'SnapKit'
